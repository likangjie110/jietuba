# 截图吧 (Jietuba) - Python 依赖
# Python 3.11 + Windows x86_64 / ARM64

# 平台标记：Windows 专用包在 macOS/Linux 上装了也没用（pywin32 根本没有非
# Windows 版本），标出来是为了让非 Windows 的开发环境能直接 pip install。

# ===== 核心依赖 =====
PySide6==6.11.0
PySideSix-Frameless-Window==0.8.1
pillow==12.1.1
mss==10.1.0
pynput==1.8.1
pywin32==311; sys_platform == "win32"
darkdetect==0.8.0
emoji==2.15.0
av==17.0.0
colorama==0.4.6
zxing-cpp==3.1.1

# ===== 自制 Rust 库（PyPI 发行名，import 名不同——见各包 README）=====
# 预编译 wheel 只发了 Windows；macOS/Linux 上要开发的话用 setup_macos.sh
# 从 rust_libs/ 本地构建（j-gif 目前是 Windows 专用的，链接了 user32/gdi32）。
j-gif==0.3.1; sys_platform == "win32"
j-stitch==0.4.0; sys_platform == "win32"
j-clipboard==0.4.1; sys_platform == "win32"
j-ppocr==0.2.0; sys_platform == "win32"
